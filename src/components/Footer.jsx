function Footer({home, serv, cont, blog}) {
    return (
        <>
            <footer className="nav">
                <a href="">{home}</a>
                <a href="">{serv}</a>
                <a href="">{cont}</a>
                <a href="">{blog}</a>
            </footer>
        </>
    )
}

export default Footer
